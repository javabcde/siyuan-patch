diff --git a/app/build.gradle b/app/build.gradle
index 50877c1..7613808 100644
--- a/app/build.gradle
+++ b/app/build.gradle
@@ -12,14 +12,23 @@ android.applicationVariants.configureEach { variant ->
         def buildType = variant.buildType.name
         def newApkName = "siyuan-${versionName}.apk"
         if (buildType != "release") {
-            def formattedDate = new Date().format('yyyyMMddHHmmss')
-            newApkName = "siyuan-${versionName}-${buildType}-${formattedDate}.apk"
+            newApkName = "app-debug.apk"
         }
         output.outputFileName = newApkName
     }
 }
 
 android {
+
+    signingConfigs {
+        debug {
+            storeFile = file("../keystore/debug.jks")
+            storePassword System.getenv("KEYSTORE_PASSWORD")
+            keyAlias "debug"
+            keyPassword System.getenv("KEYSTORE_PASSWORD")
+        }
+    }
+
     compileSdkVersion 34
     buildToolsVersion "34"
 
@@ -39,12 +48,21 @@ android {
             resValue "string", "app_package_name", "org.b3log.siyuan"
             proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
         }
-        debug {
+/*        debug {
             applicationIdSuffix ".debug"
             minifyEnabled false
             resValue "string", "app_name", "SiYuan-Debug"
             resValue "string", "app_package_name", "org.b3log.siyuan.debug"
             proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
+        }*/
+        debug {
+            applicationIdSuffix ".debug"
+            minifyEnabled true
+            shrinkResources true
+            resValue "string", "app_name", "SiYuan"
+            resValue "string", "app_package_name", "org.b3log.siyuan"
+            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
+            signingConfig signingConfigs.debug
         }
     }
 
